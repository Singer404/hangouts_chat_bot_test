{
  "name": "serverles_eobuwie__chatbot",
  "version": "0.0.1",
  "description": "Serverless hangouts chat bot ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:reminder-bot-receiver": "lerna run deploy --stream --scope=\"reminder-bot-receiver\"",
    "deploy:reminder-bot-sender": "lerna run deploy --stream --scope=\"reminder-bot-sender\""
  },
  "keywords": [
    "hangouts",
    "chat",
    "bot"
  ],
  "author": "Singer",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "dependencies": {
    "@google-cloud/firestore": "^1.3.0",
    "@google-cloud/pubsub": "^0.29.0",
    "chrono-node": "^1.3.11"
  }
}
